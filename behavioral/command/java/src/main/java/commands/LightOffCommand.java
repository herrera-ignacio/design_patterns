package commands;

import vendor.Light;

public class LightOffCommand implements UndoableCommand {
    Light light;

    public LightOffCommand(Light light) {
        this.light = light;
    }

    public void execute() {
        light.off();
    }

    public void undo() {
        light.on();
    }
}
